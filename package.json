{
  "name": "phaser-3-tilemap-blog-posts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "browser-sync start --server examples --directory",
    "deploy": "gh-pages --branch gh-pages --dist examples"
  },
  "prettier": {
    "printWidth": 100
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.5"
  },
  "dependencies": {
    "gh-pages": "^1.2.0"
  }
}
